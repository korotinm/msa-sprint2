version: "3.8"

services:
  monolith:
    platform: linux/arm64
    depends_on: [monolith-db]
    build:
      context: ../monolith
      dockerfile: Dockerfile
    container_name: hotelio-monolith
    ports:
      - "8084:8080"
    networks:
      - hotelio-net
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://monolith-db:5432/hotelio
      SPRING_DATASOURCE_USERNAME: hotelio
      SPRING_DATASOURCE_PASSWORD: hotelio
      BOOKING_SERVICE_EXTERNAL_HOST: booking-service
      BOOKING_SERVICE_EXTERNAL_PORT: 9090

  monolith-db:
    image: postgres:15
    container_name: hotelio-db
    environment:
      POSTGRES_USER: hotelio
      POSTGRES_PASSWORD: hotelio
      POSTGRES_DB: hotelio
    ports:
      - "5432:5432"
    volumes:
      - monolith-db-data:/var/lib/postgresql/data
    networks:
      - hotelio-net

  booking-service:
    platform: linux/arm64
    depends_on: [booking-db, kafka, monolith]
    build:
      context: ../..
      dockerfile: tasks/booking/Dockerfile
    container_name: booking-service
    ports:
      - "9090:9090"
    networks:
      - hotelio-net
    environment:
      BOOKING_GRPC_PORT: 9090
      BOOKING_MONOLITH_BASE_URL: http://monolith:8080
      BOOKING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      BOOKING_KAFKA_TOPIC: booking-events
      BOOKING_DB_HOST: booking-db
      BOOKING_DB_PORT: 5432
      BOOKING_DB_NAME: booking
      BOOKING_DB_USER: booking
      BOOKING_DB_PASSWORD: booking

  booking-db:
    image: postgres:15
    container_name: booking-db
    environment:
      POSTGRES_USER: booking
      POSTGRES_PASSWORD: booking
      POSTGRES_DB: booking
    ports:
      - "5433:5432"
    volumes:
      - booking-db-data:/var/lib/postgresql/data
    networks:
      - hotelio-net

  booking-history-service:
    platform: linux/arm64
    depends_on: [booking-history-db, kafka]
    build:
      context: ../..
      dockerfile: tasks/booking-history/Dockerfile
    container_name: booking-history-service
    networks:
      - hotelio-net
    environment:
      HISTORY_DB_HOST: booking-history-db
      HISTORY_DB_PORT: 5432
      HISTORY_DB_NAME: booking_history
      HISTORY_DB_USER: booking_history
      HISTORY_DB_PASSWORD: booking_history
      HISTORY_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      HISTORY_KAFKA_TOPIC: booking-events        # тот же топик, куда пишет booking-service
      HISTORY_KAFKA_GROUP_ID: booking-history-service


  booking-history-db:
    image: postgres:15
    container_name: booking-history-db
    environment:
      POSTGRES_USER: booking_history
      POSTGRES_PASSWORD: booking_history
      POSTGRES_DB: booking_history
    ports:
      - "5434:5432"
    volumes:
      - booking-history-db-data:/var/lib/postgresql/data
    networks:
      - hotelio-net

  kafka:
    depends_on: [zookeeper]
    image: confluentinc/cp-kafka:7.2.1
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
    networks:
      - hotelio-net
  zookeeper:
    image: confluentinc/cp-zookeeper:7.2.1
    ports:
      - "2181:2181"
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    networks:
      - hotelio-net

volumes:
  monolith-db-data:
  booking-db-data:
  booking-history-db-data:

networks:
  hotelio-net:
    external: true
